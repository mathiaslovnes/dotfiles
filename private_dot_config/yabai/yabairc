# launch scripting additions
yabai -m signal --add event=dock_did_restart action="sudo yabai --load-sa"
sudo yabai --load-sa
sudo /opt/homebrew/bin/yabai --load-sa

yabai -m config window_topmost off
yabai -m window --topmost off
# yabai -m config window_shadow off

# layout - bsp, stack, or float
yabai -m config layout bsp
yabai -m window --toggle float
yabai -m config --space 4 layout float

yabai -m rule --add app=".*" sub-layer=normal
yabai -m config window_opacity off
yabai -m config active_window_opacity 1.0
yabai -m config normal_window_opacity 1.0

# padding
yabai -m config top_padding 0
yabai -m config bottom_padding 0
yabai -m config left_padding 0
yabai -m config right_padding 0
yabai -m config window_gap 0

# mouse settings
yabai -m config mouse_modifier alt
yabai -m config mouse_action1 move
yabai -m config mouse_action2 resize

yabai -m mouse_drop_action swap

# sketchybar
# yabai -m config menubar_opacity 0.0 # Hide MacOS native menubar
yabai -m signal --add event=window_focused action="sketchybar --trigger window_focus"

# App-specifics
yabai -m rule --add app="^kitty$" manage=on layer=normal # Prevent starting in floating mode
yabai -m rule --add app="^System Settings$" manage=off
yabai -m rule --add app="^Karabiner-Elements$" manage=off
yabai -m rule --add app="^Karabiner-EventViewer$" manage=off
yabai -m rule --add app="^oklch-color-picker$" manage=off
yabai -m rule --add app="^Mac Mouse Fix$" manage=off
yabai -m rule --add app="^Linearmouse$" manage=off
yabai -m rule --add app="^Finder$" manage=off
# yabai -m rule --add app="^mpv$" manage=off
# yabai -m signal --add event=window_created app="mpv" \
#   action='yabai -m window $YABAI_WINDOW_ID --display 2 --grid 1:1:0:0:1:1'

yabai -m rule --add app=".*" sub-layer=normal
yabai -m rule --apply

# Hide menu-bar when MPV is open
# yabai -m signal --add event=window_focused app="mpv" \
#   action='osascript -e "tell application \"System Events\" to set autohide menu bar of dock preferences to true"'
#
# yabai -m signal --add event=window_focused app!="mpv" \
#   action='osascript -e "tell application \"System Events\" to set autohide menu bar of dock preferences to false"'

# Hide menu-bar when MPV is open (only if external display is connected)
# yabai -m signal --add event=window_focused app="mpv" \
#   action='if [ $(displayplacer list | grep -c "^Persistent screen id:") -gt 1 ]; then osascript -e "tell application \"System Events\" to set autohide menu bar of dock preferences to true"; fi'
#
# yabai -m signal --add event=window_focused app!="mpv" \
#   action='if [ $(displayplacer list | grep -c "^Persistent screen id:") -gt 1 ]; then osascript -e "tell application \"System Events\" to set autohide menu bar of dock preferences to false"; fi'

# Hide menu-bar when MPV is open (only if external display is connected) (And maximize MPV when it is focused)
# yabai -m signal --add event=window_focused app="mpv" \
#   action='if [ $(displayplacer list | grep -c "^Persistent screen id:") -gt 1 ]; then osascript -e "tell application \"System Events\" to set autohide menu bar of dock preferences to true"; fi; yabai -m window --grid 1:1:0:0:1:1'
#
# yabai -m signal --add event=window_focused app!="mpv" \
#   action='if [ $(displayplacer list | grep -c "^Persistent screen id:") -gt 1 ]; then osascript -e "tell application \"System Events\" to set autohide menu bar of dock preferences to false"; fi'


