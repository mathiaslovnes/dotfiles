My dotfiles

# Note to self:
Chezmoi sometimes will not note a diff of added files inside a directory.
It might not notify me of plugins added to nvim/lua/plugins being untracked, even though the parent directory is tracked - periodically run 'chezmoi add ~/.config/nvim/lua/plugins'.
